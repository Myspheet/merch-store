import { ResourceInterceptor } from './resource.interceptor';

describe('ResourceInterceptor', () => {
  it('should be defined', () => {
    expect(new ResourceInterceptor()).toBeDefined();
  });
});
